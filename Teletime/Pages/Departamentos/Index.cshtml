@page
@model Teletime.Pages.Departamentos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Departamento[0].NomeDepartamento)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Departamento) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeDepartamento)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdDepartamento">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdDepartamento">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdDepartamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
